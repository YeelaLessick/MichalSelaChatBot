# Dashboard dependencies
# Install with:  pip install -r dashboard/requirements.txt

streamlit>=1.30.0
plotly>=5.18.0
pandas>=2.0.0
psycopg[binary]>=3.1
azure-identity>=1.15.0   # only needed when POSTGRES_USE_AAD=true
azure-storage-blob>=12.22.0
python-dotenv>=1.0.0
